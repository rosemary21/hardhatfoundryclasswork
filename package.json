{
  "name": "greeter-celo-sepolia",
  "version": "1.0.0",
  "description": "Greeter contract deployed to Celo Sepolia using Hardhat and Foundry",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:hardhat": "hardhat run scripts/deploy.js --network celoSepolia",
    "test:hardhat": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
